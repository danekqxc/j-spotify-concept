<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/music_player_bar"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:background="@drawable/player_bar_background"
    android:elevation="8dp"
    android:paddingStart="12dp"
    android:paddingEnd="12dp">

    <!-- Кнопка воспроизведения -->
    <ImageView
        android:id="@+id/btn_play_pause"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@android:drawable/ic_media_play"
        app:tint="#FFFFFF"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <LinearLayout
        android:id="@+id/text_container"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="8dp"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/progress_wrapper"
        app:layout_constraintStart_toEndOf="@+id/btn_play_pause"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/track_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:singleLine="true"
            android:text="не воспроизводится"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/track_artist"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:singleLine="true"
            android:text="неизвестен"
            android:textColor="#B3B3B3"
            android:textSize="11sp" />
    </LinearLayout>

    <!-- Обновленный блок прогресса с волной слева -->
    <LinearLayout
        android:id="@+id/progress_wrapper"
        android:layout_width="140dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginEnd="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/btn_favorite"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Увеличенная волна -->
        <LinearLayout
            android:id="@+id/wave_container"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:layout_marginEnd="10dp"
            android:gravity="bottom"
            android:orientation="horizontal">
            <View android:id="@+id/wave_bar1" android:layout_width="3dp" android:layout_height="10dp" android:layout_marginEnd="2dp" android:background="@color/spotify_green" />
            <View android:id="@+id/wave_bar2" android:layout_width="3dp" android:layout_height="20dp" android:layout_marginEnd="2dp" android:background="@color/spotify_green" />
            <View android:id="@+id/wave_bar3" android:layout_width="3dp" android:layout_height="8dp" android:layout_marginEnd="2dp" android:background="@color/spotify_green" />
            <View android:id="@+id/wave_bar4" android:layout_width="3dp" android:layout_height="15dp" android:background="@color/spotify_green" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <ProgressBar
                android:id="@+id/track_progress_horizontal"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="4dp"
                android:progressDrawable="@android:drawable/progress_horizontal"
                android:progressTint="@color/spotify_green" />

            <TextView
                android:id="@+id/track_time"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="4dp"
                android:text="00:00 / 00:00"
                android:textColor="#B3B3B3"
                android:textSize="9sp" />
        </LinearLayout>
    </LinearLayout>

    <ImageView
        android:id="@+id/btn_favorite"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:src="@drawable/ic_heart"
        app:tint="#B3B3B3"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>